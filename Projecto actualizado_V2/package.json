{
  "name": "doarfazbem",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend"
  },
  "workspaces": [
    "backend",
    "frontend"
  ]
}